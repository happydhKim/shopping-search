# Grafana datasource auto-provisioning
# Prometheus를 기본 datasource로 자동 등록

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
